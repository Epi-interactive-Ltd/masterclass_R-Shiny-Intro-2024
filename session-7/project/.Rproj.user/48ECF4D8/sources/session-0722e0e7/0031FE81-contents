# Stage 1
ui <- fluidPage(
  # Insert UI Components here #
  titlePanel("Session 7"),
  
  sidebarLayout(
    sidebarPanel(
      h2("Filter:"),
      # Add selectInputs for region, subregion, and country
    ),
    
    mainPanel(
      tabsetPanel(
        # Add tabPanels for map, graph, and table
          
      )
    )
  )
  
)